import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

/*This is the starting point. I.e. the first code that will be executed in the browser when we refresh.

<App /> is called JSX. It is not understood by browsers instead it is converted to real JS code. */
